# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# ── Spring Backend ────────────────────────────────────────────────────────────
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/<POSTGRES_DB>
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=

JWT_SECRET=
JWT_EXPIRATION_MS=

# Comma-separated — set to your domain in production (e.g. https://your-domain.com)
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000

# ── Frontend ──────────────────────────────────────────────────────────────────
# Browser-side API base URL — goes through nginx on port 80
NEXT_PUBLIC_API_URL=http://localhost/api